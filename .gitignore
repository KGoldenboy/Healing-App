# dist/ is committed (pre-built single-file app so users needn't build it).
# BUT the local `healing-app.html` symlink (> dist/healing-app.html) is a
# machine-specific absolute path — never commit it.
/healing-app.html

cache/
data/*.bak.*
.pytest_cache/
__pycache__/
*.pyc
.env
.env.*
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db
